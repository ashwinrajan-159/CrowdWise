"""Web layer for CrowdWise — FastAPI backend + Leaflet/upload frontend.

Imports the existing gridlock/ pipeline, predict.py, and scrape_events.py as
libraries. The ML pipeline is unchanged; this package only serves it over HTTP.
"""
